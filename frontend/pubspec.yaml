name: trip_split
description: Trip planning and shared-expense app for Android.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.13.2
  flutter: ">=3.47.0"

dependencies:
  cloud_firestore: ^6.9.0
  cloud_functions: ^6.4.0
  firebase_auth: ^6.6.1
  firebase_core: ^4.14.0
  flutter:
    sdk: flutter
  google_maps_flutter: 2.18.0
  google_sign_in: ^7.2.0

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
