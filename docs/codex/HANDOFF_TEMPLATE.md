# 작업 인계 템플릿

아래 여섯 칸을 채운 뒤 작업을 넘긴다. 모르는 값은 추측하지 말고 `확인 필요`라고 적는다. 명령은 저장소 루트에서 실행한 것을 기준으로 적는다.

## 1. 목표

- 작업명:
- 이번 작업에서 끝내려는 관찰 가능한 결과:
- 범위에 포함하지 않은 것:
- 관련 문서·결정:

## 2. 끝난 것

- 구현 또는 문서 변경:
- 동작 확인한 경로/시나리오:
- 남아 있는 알려진 제한:

## 3. 남은 것

- [ ] 항목 — 완료 조건:
- [ ] 항목 — 완료 조건:
- 막힌 항목과 막힌 이유:

## 4. 결정한 것과 이유

| 결정 | 이유·근거 | 영향받는 경로 | 되돌릴 조건 |
| ---- | --------- | ------------- | ----------- |
|      |           |               |             |

문서 간 충돌을 해소했다면 최신 결정 날짜, 구현 위치, 검증 결과를 함께 적는다.

## 5. 하지 말 것

- 이번 인계 범위에서 변경하지 않을 파일·기능:
- 아직 연결하지 않은 외부 API·배포·secret:
- 검증 조건을 약화시키거나 삭제하면 안 되는 항목:

## 6. 확인 방법

실행 환경:

- Node.js:
- data source (`mock`/`firebase`):
- Firebase Emulator 프로젝트·서비스(해당 시):

실행한 명령과 결과:

```text
npm ci
pwsh -File scripts/verify.ps1 -Mode Fast
pwsh -File scripts/verify.ps1 -Mode Full
```

- 각 명령의 종료 코드:
- 실패 또는 전제조건 미충족의 첫 관련 오류:
- 수동 확인 경로 또는 화면 크기:
- 다음 작업자가 가장 먼저 재현할 명령:
