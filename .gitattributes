# Match the repository's Prettier endOfLine=lf contract on every platform.
* text=auto eol=lf

# Windows command wrappers require CRLF when the repository adds them.
*.bat text eol=crlf
*.cmd text eol=crlf
